# `npm version <patch|minor|major>` erzeugt einen git-Tag mit "v"-Präfix
# (z. B. v0.0.81), passend zur bestehenden Tag-Konvention auf main.
tag-version-prefix=v

# Commit-/Tag-Message des Version-Bumps. %s = neue Versionsnummer (ohne "v").
message=chore(release): v%s
